language: node_js
node_js:
  - "4.4"

before_script:
  - npm install -g grunt grunt-cli bower typings --progress=false
  - bower install --force-latest
  - typings install

script: 
  - grunt dist
  - grunt travis-karma
