background: '#FFF'
textAlignment: left

toolbar:
    background: !palette grey.color
    text: !palette grey.text

header:
    background: !palette light.color|light
    borderColor: !palette light.color
    text: !palette light.text|light
    cellPaddingTop: 2em
    fontWeight: normal
    textAlignment: left

rows:
    zebraColor: !palette light.color|light
    hoverColor: !palette primary.color|x-light

cell:
    minWidth: 5em
    padding: 0.5em 1em
