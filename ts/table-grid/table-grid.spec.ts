/// <reference path="../references.spec.ts" />

module Lui.Directives.TableGrid.Test {

	"use strict";

	beforeEach(angular.mock.module("lui.directives"));

	describe("Table Grid", () => {
		it("bogus should fail", () => {
			expect(false).toBeTruthy();
		});
	});
}